// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  base: '/studio-mua',  // esatto nome del repo
  site: 'https://fmax95.github.io',
});
